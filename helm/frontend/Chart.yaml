apiVersion: v2
name: frontend
description: VM Order Portal — frontend static site + reverse proxy (nginx)
type: application
version: 0.1.0        # chart version
appVersion: "1.0.0"   # image version it deploys by default
